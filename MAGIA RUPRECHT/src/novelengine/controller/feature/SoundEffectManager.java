package novelengine.controller.feature;

import java.util.Map;

/**
 * SoundEffectManager
 * 効果音（SE）の再生を担当。
 */
public class SoundEffectManager {

    public void playSE(Map<String, Object> seData) {
        // TODO: 効果音再生処理
        // 例: ファイル名、音量、再生タイミング(delay)など
        System.out.println("SE再生：" + seData);
    }
}
